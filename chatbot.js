function toggleChat(){document.getElementById('chatBox').classList.toggle('hidden');}
function appendChat(text,type){const box=document.getElementById('chatMessages');const bubble=document.createElement('div');bubble.className=type==='user'?'user-bubble':'bot-bubble';bubble.textContent=text;box.appendChild(bubble);box.scrollTop=box.scrollHeight;}
function getBotReply(text){const q=text.toLowerCase().trim();if(/^(hi|hello|hey|hii|namaste)\b/.test(q))return'Hello! 👋 I can help you with PAN, Aadhaar, Passport and Driving Licence services. Tell me which service you need.';if(q.includes('thank'))return'You’re welcome! 😊 If you need another service or want to check documents, I’m here to help.';if(q.includes('pan'))return'For PAN, keep identity, address and date-of-birth proof ready. Use the PAN guide, then continue to the official Income Tax portal for the actual application.';if(q.includes('aadhaar')||q.includes('aadhar'))return'For Aadhaar, the exact process depends on the service. Some updates may require an authorised centre. Use the Aadhaar guide and verify the current UIDAI instructions.';if(q.includes('passport'))return'For Passport Seva, choose fresh passport or reissue, complete the official application, pay the applicable fee and book an appointment when required.';if(q.includes('driving')||q.includes('licence')||q.includes('license')||q==='dl')return'For Driving Licence services, use the official Parivahan/Sarathi system. The requirements depend on learner, permanent licence, renewal and your state.';if(q.includes('document'))return'Open Services → choose a guide → Documents Required. The checklist helps you prepare before you open the official portal.';if(q.includes('apply'))return'Choose your service, read the documents and steps, then use Apply Online to open the official government destination.';if(q.includes('error')||q.includes('reject')||q.includes('mistake'))return'Check your details against your supporting documents, make sure required fields and uploads are complete, and use the official portal for the latest correction process.';return'I can help with PAN, Aadhaar, Passport, Driving Licence, documents and application steps. Try asking “PAN documents” or “How do I apply?”';}
function sendChatMessage(){const input=document.getElementById('chatInput');const text=input.value.trim();if(!text)return;appendChat(text,'user');input.value='';setTimeout(()=>appendChat(getBotReply(text),'bot'),250);}
function sendQuickChat(text){appendChat(text,'user');setTimeout(()=>appendChat(getBotReply(text),'bot'),180);}
