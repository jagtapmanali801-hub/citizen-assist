function renderChecklist(steps){const c=document.getElementById('checklistContainer');c.innerHTML='';steps.forEach((step,i)=>{const label=document.createElement('label');label.className='check-item';label.innerHTML=`<input type="checkbox" class="checklist-item" onchange="calculateChecklistProgress()"><span><strong>Step ${i+1}:</strong> ${step}</span>`;c.appendChild(label);});calculateChecklistProgress();}
function calculateChecklistProgress(){const items=[...document.querySelectorAll('.checklist-item')];const done=items.filter(x=>x.checked).length;document.getElementById('guideProgressPercent').textContent=(items.length?Math.round(done/items.length*100):0)+'%';}
